import styles from '@/styles/wrapper.module.css'
import { FollowPointerFramerMotion  } from '@/components/FollowPointerFramerMotion'

export default function FollowPointerFramerMotionPage() {
  return <main className={`${styles.wrapper}`}><FollowPointerFramerMotion /></main>
}
