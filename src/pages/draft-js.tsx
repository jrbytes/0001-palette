import styles from '@/styles/Home.module.css'
import { DraftJS } from '@/components/Draft-js'

export default function DraftJSPage() {
  return <main className={`${styles.main}`}><DraftJS /></main>
}
