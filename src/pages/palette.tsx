import styles from '@/styles/wrapper.module.css'
import { Palette } from '@/components/Palette/Palette'

export default function PalettePage() {
  return <main className={`${styles.wrapper}`}><Palette /></main>
}
