import styles from '@/styles/Home.module.css'
import { Palette } from '@/components/Palette/Palette'

export default function PalettePage() {
  return <main className={`${styles.main}`}><Palette /></main>
}
